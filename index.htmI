<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Imprinta — VIP</title>
  <meta name="description" content="Imprinta — премиальная печать и VIP сервис. Быстрый заказ и связь через телефон и Telegram." />

  <style>
    :root{
      --bg:#07080b;
      --panel:rgba(255,255,255,.06);
      --stroke:rgba(255,255,255,.12);
      --text:rgba(255,255,255,.90);
      --muted:rgba(255,255,255,.62);
      --gold1:#f7d37b;
      --gold2:#caa24d;
      --shadow: 0 18px 60px rgba(0,0,0,.55);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Inter,Arial,sans-serif;
      background:
        radial-gradient(900px 600px at 20% -10%, rgba(247,211,123,.18), transparent 60%),
        radial-gradient(900px 700px at 110% 10%, rgba(202,162,77,.14), transparent 55%),
        radial-gradient(900px 700px at 50% 110%, rgba(255,255,255,.06), transparent 55%),
        var(--bg);
      color:var(--text);
      min-height:100vh;
    }
    .wrap{max-width:980px;margin:0 auto;padding:28px 18px 60px}
    .top{
      display:flex;align-items:center;justify-content:space-between;gap:14px;
      padding:10px 0 22px;
    }
    .brand{
      display:flex;align-items:center;gap:12px;
    }
    .logo{
      width:44px;height:44px;border-radius:14px;
      background: linear-gradient(135deg, rgba(247,211,123,.95), rgba(202,162,77,.95));
      box-shadow: 0 10px 28px rgba(202,162,77,.22);
    }
    .brand h1{margin:0;font-size:18px;letter-spacing:.
